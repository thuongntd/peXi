import { Box } from "@mui/system";
import * as React from "react";

export const Logo = () => {
  return (
    <Box
      component="h1"
      sx={{
        color: "rgb(122 216 227)",
        fontWeight: "bold",
        textTransform: "uppercase",
      }}
    >
      Logo ne
    </Box>
  );
};
